# Карта консолидации движков входящих ЛС

> Решение владельца: **AI Агент — единственный движок обработки входящих ЛС.**
> AI Sales снять с рантайма (фишки воронки перенести, если дёшево; иначе данные сохранить, функцию отложить).
> auto-replier — опциональная дешёвая фронт-линия, которая ПЕРЕДАЁТ диалог Агенту (не отвечает параллельно).
>
> Этап A — только анализ. Источники: `app/tasks/{ai_agent,ai_sales,auto_replier}_tasks.py`,
> `app/api/v1/{ai_sales,auto_replier}.py`, `app/api/v1/ai_promoter.py` (контроллер Агента),
> `public/index.html`, `schema.sql`, `app/tasks/{beat_schedule,celery_app}.py`, `app/core/database.py`.
> Дата: 2026-06-03. Ветка `wip/migration`.

---

## 0. Корень конфликта — как каждый движок забирает входящие ЛС

**Главное:** ни один из трёх движков НЕ в `beat_schedule.py`. Все три — **самоперепланирующиеся
Celery-петли** (`apply_async(countdown=…)`), регистрируются импортом в `celery_app.py`, стартуют
по API и сами себя перевыпускают, пока есть активные строки. Все трое опрашивают историю одинаково:
`client.get_dialogs(limit=30)` → фильтр `is_user` + `unread_count>0` → `get_messages(limit=unread_count)`
→ обработка инбаунда → `send_read_acknowledge`. **Нет watermark по `last_msg_id`** (кроме секретаря
Агента) — дедуп держится на `unread_count` Телеграма. **Нет кросс-движкового лока по `peer_id`.**

| Движок           | Task                      | Старт                                                                                           | Интервал                                                                 | Где в коде                                                                                     |
| ---------------- | ------------------------- | ----------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------ | ---------------------------------------------------------------------------------------------- |
| **auto-replier** | `pup_tg.auto_replier`     | `POST /auto-replier/scenarios/{id}/activate` → `dispatch_task` (auto_replier.py:263)            | **90s** (`_RESCHEDULE_SECONDS=90`, auto_replier_tasks.py:111)            | петля auto_replier_tasks.py:131-141; опрос :252-284                                            |
| **AI Sales**     | `pup_tg.ai_sales_monitor` | `POST /ai-sales/scripts/{id}/start` (ai_sales.py:434) или `POST /ai-sales/monitor` (:512)       | **90s** (`_MONITOR_RESCHEDULE_SECONDS=90`, ai_sales_tasks.py:422)        | петля ai_sales_tasks.py:425-446; опрос :540-568. Доп. on-demand `pup_tg.ai_sales_reply` (:802) |
| **AI Агент**     | `pup_tg.ai_agent`         | `POST /ai-promoter/personas/{id}/activate` (ai_promoter.py:342) + `ai_agent_reaper` (beat 5мин) | **`scan_interval_sec` def 180s** (15..3600, ai_agent_tasks.py:1418-1423) | петля :1386-1423; **секретарь ЛС** `_handle_secretary_dm` :1097-1301; опрос :2500-2566         |

**Конфликт:** если на одном аккаунте одновременно активны персона Агента (dm_enabled=1) и
сценарий auto-replier и/или скрипт AI Sales — каждая петля независимо прочитает один и тот же
непрочитанный инбаунд и ответит. Один `send_read_acknowledge` гасит `unread_count`, поэтому возможны
гонки и двойные/потерянные ответы. **Единственная координация сейчас:** флаг `muted` в `tg_dm_threads`
(только Агент) и проверка терминальных статусов `tg_sales_dialogs` (Агент уступает sales-flow).

### Текущее ЖИВОЕ состояние (ws-default.db — единственная БД)

- 1 персона **Atlas — мягкий прогрев** · ACTIVE · `dm_enabled=1`, `dm_reply_to_all=1` → **Агент уже владеет ЛС** (3 строки `tg_dm_threads`).
- **0** sales-скриптов, **0** sales-диалогов, **0** sales-сообщений → AI Sales де-факто НЕ работает (петля сама останавливается при 0 ACTIVE-скриптов).
- **0** сценариев auto-replier → auto-replier НЕ работает.
- 10 аккаунтов (9 ACTIVE + 1 INVALID).

**Вывод:** на проде конфликт сейчас НЕ горит — реально крутится только Агент. Консолидация =
(1) закрепить единоличное владение архитектурно, чтобы конфликт не возник при будущей активации
ai_sales/auto-replier рядом с Агентом, и (2) перепрофилировать auto-replier во фронт-линию.
**Удаление AI Sales не теряет живых данных** (0 строк), таблицы всё равно НЕ дропаем.

---

## 1. AI SALES — построчная карта (что переносим / убираем / уже есть)

### Возможности

| Возможность                                                       | Решение                                              | Комментарий                                                                                                                                                                                                                                                                             |
| ----------------------------------------------------------------- | ---------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Опрос входящих ЛС (петля 90s)                                     | **УБРАТЬ (рантайм)**                                 | Дублирует петлю Агента. Снять с рантайма.                                                                                                                                                                                                                                               |
| Ответы на инбаунд по истории диалога                              | **УЖЕ ЕСТЬ в Агенте**                                | `_handle_secretary_dm` + продолжение по существующим `tg_sales_dialogs` (ai_agent_tasks.py:2528-2731).                                                                                                                                                                                  |
| Keyword-RAG (top-3 чанка из `tg_kb_chunks`)                       | **УЖЕ ЕСТЬ в Агенте**                                | `_search_kb_chunks` (ai_agent_tasks.py:469-532). Перенос не нужен — общий KB.                                                                                                                                                                                                           |
| Бюджет/трекинг стоимости AI                                       | **УЖЕ ЕСТЬ в Агенте**                                | `tg_settings.ai_*` пишут оба.                                                                                                                                                                                                                                                           |
| Анти-бан (NO_PROXY, FloodWait→статус)                             | **УЖЕ ЕСТЬ в Агенте**                                | Идентичная логика.                                                                                                                                                                                                                                                                      |
| Память диалога (история → Claude)                                 | **УЖЕ ЕСТЬ в Агенте**                                | Секретарь держит rolling-summary + историю.                                                                                                                                                                                                                                             |
| Хэндофф человеку (`HANDED_OFF`)                                   | **УЖЕ ЕСТЬ** (терминальный статус уважается Агентом) | endpoint можно оставить read-only.                                                                                                                                                                                                                                                      |
| **Скрипты продаж** (`tg_sales_scripts`: system_prompt+stages+rag) | **ОТЛОЖИТЬ + сохранить данные**                      | На проде 0 строк. Хранение тривиально, но движок stage-машины — нет.                                                                                                                                                                                                                    |
| **Воронка / стадии** (keyword-driven `_evaluate_stage_advance`)   | **ОТЛОЖИТЬ (P6-09)** + сохранить данные              | **Единственная функция, которой НЕТ у Агента.** Агент создаёт лид со стадией `intro` и двигает только `lead_status`, но НЕ прогрессирует стадии по скрипту (ai_agent_tasks.py:2367-2380). Перенос = MEDIUM (bespoke стейт-машина). Per решение владельца — отложить, данные не дропать. |
| Статистика/конверсия (`/stats`)                                   | **УБРАТЬ или read-only**                             | Дублирует `/ai-promoter/stats`.                                                                                                                                                                                                                                                         |

### Эндпоинты `app/api/v1/ai_sales.py` (prefix `/ai-sales`)

| Метод/путь                                                                                         | Решение                                                                                                                              |
| -------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------ |
| `GET /scripts`, `GET /dialogs`, `GET /dialogs/{id}`, `GET /stats`, `GET /knowledge-base`           | **ОСТАВИТЬ read-only** (архив/просмотр)                                                                                              |
| `POST /scripts`, `PATCH /scripts/{id}`, `DELETE /scripts/{id}`                                     | **ОСТАВИТЬ** (правка отложенных скриптов не вредит) или заморозить — согласовать                                                     |
| `POST /scripts/{id}/start`, `POST /scripts/{id}/stop`, `POST /monitor`, `POST /dialogs/{id}/reply` | **СНЯТЬ С РАНТАЙМА**: `/start` и `/monitor` не должны запускать петлю, когда активен Агент-владелец ЛС → возвращать 409/no-op + лог. |
| `POST /dialogs/{id}/handoff`                                                                       | **ОСТАВИТЬ** (ставит `HANDED_OFF`, уважается Агентом)                                                                                |

### UI (`public/index.html`, секция `ai-sales` «AI Продажник», `rAiSales` :5053)

- 4 вкладки: Скрипты / Диалоги / Воронка / База знаний. UI **никогда не вызывает** `/start`,`/stop`,`/monitor`,`/reply`,`/handoff` (только list/create/delete/view).
- **Решение:** сделать раздел **read-only / архив** (баннер «Движок консолидирован в AI Агенте; раздел в режиме просмотра»). Список/диалоги/воронка/KB остаются видимыми. Create/Delete — убрать или оставить как редактор отложенных скриптов (согласовать). UI не ломаем.

### Таблицы БД

| Таблица                                                                 | Владелец                         | Решение                                                          |
| ----------------------------------------------------------------------- | -------------------------------- | ---------------------------------------------------------------- |
| `tg_sales_scripts` (schema:507) — скрипты/воронки/стадии, **user-data** | только AI Sales                  | **НЕ ДРОПАТЬ.** Хранить. На проде 0 строк.                       |
| `tg_sales_dialogs` (schema:524) — лиды/стадии                           | **СО-ВЛАДЕНИЕ AI Sales + Агент** | **НЕ ДРОПАТЬ** — Агент пишет сюда (ai_agent_tasks.py:2307…2680). |
| `tg_sales_messages` (schema:544) — лог переписки                        | **СО-ВЛАДЕНИЕ AI Sales + Агент** | **НЕ ДРОПАТЬ** — Агент пишет сюда.                               |

> ⚠️ Нельзя дропнуть sales-таблицы: `tg_sales_dialogs`/`tg_sales_messages` — рабочие таблицы Агента.

### Shared state

AI Sales и Агент пишут одни `tg_sales_dialogs`/`tg_sales_messages` по ключу `(account_id, contact_user_id)` / `dialog_id`. `tg_sales_scripts` — только AI Sales.

---

## 2. AUTO-REPLIER — построчная карта

### Возможности

| Возможность                                                            | Решение                         | Комментарий                                                                     |
| ---------------------------------------------------------------------- | ------------------------------- | ------------------------------------------------------------------------------- |
| Опрос входящих ЛС (петля 90s, параллельно Агенту)                      | **УБРАТЬ параллельный опрос**   | Перепрофилировать: не отвечать параллельно с Агентом.                           |
| Детерминированный триаж (any/keyword/regex триггеры)                   | **ОСТАВИТЬ как фронт-линию**    | Дешёвый pre-filter перед AI Агента.                                             |
| `TEMPLATE` (статичный ответ)                                           | **ОСТАВИТЬ (фронт-линия)**      | Дешёвый ответ без LLM.                                                          |
| `AI_REPLY` (Haiku короткий ответ)                                      | **ПЕРЕСМОТРЕТЬ**                | Дублирует AI Агента → лучше не нужен, оставить TEMPLATE/SILENCE/NOTIFY/HANDOFF. |
| `SILENCE` / `NOTIFY`                                                   | **ОСТАВИТЬ**                    | Дешёвая фильтрация/уведомление.                                                 |
| `HANDOFF_SALES` (сейчас **stub no-op**, auto_replier_tasks.py:330-332) | **ПЕРЕДЕЛАТЬ в HANDOFF→Агенту** | Сейчас ничего не делает. Стать точкой передачи диалога Агенту.                  |
| Анти-бан (active-hours, delay, NO_PROXY, health-переходы)              | **ОСТАВИТЬ**                    | Корректно.                                                                      |

### Эндпоинты `app/api/v1/auto_replier.py` (prefix `/auto-replier`)

| Метод/путь                                                 | Решение                                                                           |
| ---------------------------------------------------------- | --------------------------------------------------------------------------------- |
| `GET /scenarios`, `GET /log`, `POST /test`, CRUD scenarios | **ОСТАВИТЬ**                                                                      |
| `POST /scenarios/{id}/activate`                            | **ОСТАВИТЬ, но петля не отвечает параллельно с Агентом** (single-ownership gate). |
| `POST /scenarios/{id}/pause`                               | **ОСТАВИТЬ**                                                                      |

### UI (секция `auto-replier` «Автоответчик», `rAutoReplier` :5350)

- **ОСТАВИТЬ.** Поправить tooltip `HANDOFF_SALES` → «передать AI Агенту» (сейчас «передать AI Продажнику», index.html:5469). Возможно убрать опцию `AI_REPLY` из селекта.

### Таблицы

| Таблица                                  | Решение                                    |
| ---------------------------------------- | ------------------------------------------ |
| `tg_auto_replier_scenarios` (schema:637) | **ОСТАВИТЬ** (данные/сценарии НЕ удалять). |
| `tg_auto_replies` (schema:652)           | **ОСТАВИТЬ** (лог).                        |

### Shared state

**Нет пересечения** с sales/agent-таблицами. Пишет только свои + `tg_accounts.status` + `tg_audit_logs`.

---

## 3. AI АГЕНТ — что НЕ сломать (уже работает)

- Участие в групп-чатах (PHASE A, ai_agent_tasks.py:1626-2061).
- Проактивный опенер/оживление тихих чатов (`_generate_opener` :205-256).
- Эмодзи-реакции (:259-270).
- KB/RAG (`_search_kb_chunks` :469-532).
- Скоринг лида (`_evaluate_interest` :1333-1379) → ЛС-аутрич (PHASE B1 :2294-2492).
- **Секретарь входящих ЛС** (`_handle_secretary_dm` :1097-1301) — уже умеет владеть `(account_id, peer_id)` через `tg_dm_threads`, watermark `last_summarized_msg_id`, mute-гейт.
- Self-learning / style bank (:550-664).
- Self-loop + `loop_token` (единственная живая петля) + reaper-ревайв.

> Atlas-персона ЖИВАЯ. Любое изменение Агента — с максимальной осторожностью + live-проверка после шага.

---

## 4. Прочие интеграции

- **`connect_ai_sales`** (dm-campaign wizard, index.html:5752/5792/5841) — **фантомный флаг**: хранится в config кампании, но **бэкенд его не читает** (нет потребителей в `app/`). Решение: переименовать ярлык в «Подключить AI Агента при ответе» (или скрыть) — поведенчески безопасно, т.к. Агент-секретарь и так отвечает на любой инбаунд.

---

## 5. BLOCKED-вопросы владельцу

**Жёстких блокеров НЕТ.** Обоснование по критерию владельца («стоп, если удаление AI Sales теряет
данные или незаменимую функцию»):

- Потери данных нет: на проде 0 sales-строк, и таблицы мы НЕ дропаем.
- Единственная функция-пробел — **скриптовая воронка со стадиями**. Решение владельца уже покрывает
  этот случай: «иначе данные сохранить, функцию пометить как отложенную» → откладываем в **P6-09**,
  данные сохраняем. Самостоятельного продуктового решения тут не требуется.

**Принятые по ходу решения (отменишь — скорректирую, не блокер):**

1. auto-replier как фронт-линия реализуется через **single-ownership gate**: когда на аккаунте активна
   персона Агента с `dm_enabled`, auto-replier НЕ опрашивает/не отвечает параллельно. `HANDOFF_SALES`
   переделывается в передачу Агенту (сейчас это no-op stub — потерь нет).
2. AI Sales UI → **read-only/архив** с баннером; sales-таблицы сохраняются; стадии-воронка → P6-09.
3. `connect_ai_sales` — переименовать ярлык на «AI Агент».

---

## 6. План Этапа B — ✅ ВЫПОЛНЕН (B1-B4)

> Статус: B1 (commit 4264ba6), B2-B4 (commit B2-B3). Регресс 26/26, contract_smoke ✓, Atlas ACTIVE.
> Подробности и галочки — в [WORK-PLAN.md](WORK-PLAN.md) под P5-05.

- **B1.** Single-ownership: гейт на петлю/эндпоинты AI Sales (`/start`,`/monitor`) и auto-replier —
  не опрашивать ЛС параллельно с активным Агентом-владельцем. (закрывает корень конфликта; на проде
  превентивно, т.к. конкурентов 0). → частично закрывает P5-05.
- **B2.** auto-replier → фронт-линия: `HANDOFF_SALES` = передача Агенту; убрать параллельный ответ;
  tooltip/опции UI поправить. Данные auto-replier не трогаем.
- **B3.** AI Sales off-runtime: `/start`+`/monitor` no-op/409; UI read-only-баннер; `connect_ai_sales`
  переименовать; воронку отложить (P6-09); таблицы сохранить.
- **B4.** Регресс `regression_screens.py` + `contract_smoke.py`; Atlas вернуть в исходный ACTIVE-статус.

> Остаются вне этого скоупа: P5-08, P5-09, Phase 6.
