# PLAN — TG Service (аудит + работы на ~2ч)

> Дата: 2026-06-02. Метод аудита: запущенный dev-сервер (uvicorn :8001), живой прогон
> всех 25 экранов SPA через headless Chrome (playwright) + прозвон всех основных API
> эндпоинтов с admin-токеном + проверка create/start-флоу. Не по коду — вживую.

---

## ЭТАП 1 — Результаты аудита

### Главный вывод

UI и CRUD (создание/чтение/редактирование сущностей) **работают во всех разделах** —
ни один экран не падает с JS/console/network-ошибкой, все основные `GET` отдают `200`,
`POST .../create` отдают `201`. НО:

**🔴 Движок фоновых задач не работает локально.** Redis не установлен в системе
(`redis-server` отсутствует, brew/pm2 нет), процесс Celery-воркера не запущен
(в `ps` только uvicorn-api). При этом каждый эндпоинт `/start` переводит задачу в
`ACTIVE`/`RUNNING`, пытается `celery_app.send_task(...)`, **глушит исключение**
(`log.warning("celery_dispatch_skipped")`) и возвращает успех. Итог: пользователь
жмёт «Старт», задача показывается активной, но **никто её не исполняет** и прогресса нет.
Это делает «частично нерабочими» ВСЕ разделы с фоновым исполнением (парсер, прогрев,
рассылки, накрутка, комментинг, AI-агенты, инвайтинг, клонер, конвертер).

Воркеры при этом **написаны и реальны** (не заглушки): `commenting_tasks.py` — 691 стр.,
`ai_sales_tasks.py` — 1032 стр. (заметки в DEBT-PLAN об «недописанных воркерах» устарели).
Проблема не в коде воркеров, а в том, что их **нечем запустить** (нет брокера+воркера).

Проверено: Celery 5.4 есть в venv; kombu 5.6 есть → **filesystem-брокер Celery работает
локально без Redis вообще** (проверил end-to-end: задача исполнилась воркером). Это и есть
путь поднять движок локально.

### Аудит по разделам

| Раздел                    | Статус      | Детали                                                                                            |
| ------------------------- | ----------- | ------------------------------------------------------------------------------------------------- |
| Дашборд                   | ✅ работает | stats отдаются, рендер ок                                                                         |
| Аккаунты                  | ✅ работает | список/стат/CRUD ок (импорт не тестился вживую — нужны файлы)                                     |
| Прокси                    | ✅ работает | CRUD/stats ок                                                                                     |
| Парсер аудитории          | ⚠️ частично | создание задачи ок (валидация `mode`/`config` корректна); **Старт не исполняется** (нет воркера)  |
| Базы аудитории            | ✅ работает | CRUD ок                                                                                           |
| База каналов              | ✅ работает | list/resolve ок                                                                                   |
| Чекер номеров             | ✅ работает | list ок                                                                                           |
| AI Агент (промоутер)      | ⚠️ частично | CRUD персон ок; activity требует `persona_id` (SPA передаёт — не баг); **фон не исполняется**     |
| Арена агентов             | ⚠️ частично | `/arenas` ок; старт арены зависит от воркера                                                      |
| База знаний               | ✅ работает | документы/поиск/stats ок; crawl зависит от воркера                                                |
| Диалоги ЛС (AI продажник) | ⚠️ частично | CRUD скриптов ок; **старт скрипта не исполняется** (нет воркера); RAG-цепочка не проверена вживую |
| Нейрокомментинг           | ⚠️ частично | создание задачи ок; **Старт не исполняется** (нет воркера)                                        |
| Автоответчик              | ✅/⚠️       | CRUD сценариев ок; реакция на входящие зависит от воркера/клиента                                 |
| Прогрев                   | ⚠️ частично | статус/скрипты ок; не прогонялся на живых аккаунтах (DEBT 2.4) + зависит от воркера               |
| Рассылка в ЛС             | ⚠️ частично | создание кампании ок; **Старт не исполняется**                                                    |
| Рассылка по чатам         | ⚠️ частично | CRUD ок; **Старт не исполняется**                                                                 |
| Вступление в чаты         | ⚠️ частично | CRUD ок; **Старт не исполняется**                                                                 |
| Инвайтинг                 | ⚠️ частично | CRUD ок; **Старт не исполняется**                                                                 |
| Шаблоны                   | ✅ работает | CRUD ок (валидация категорий корректна)                                                           |
| Накрутка                  | ⚠️ частично | CRUD ок; **Старт не исполняется**                                                                 |
| Масслайкинг сторис        | ⚠️ частично | CRUD ок; **Старт не исполняется**                                                                 |
| Клонер                    | ⚠️ частично | CRUD ок; **Старт не исполняется**                                                                 |
| Создание чатов            | ⚠️ частично | CRUD ок; **Старт не исполняется**                                                                 |
| Конвертер                 | ⚠️ частично | CRUD ок; **Старт не исполняется**                                                                 |
| Мессенджер                | ✅ работает | рендер ок (живой TG-клиент требует аккаунт)                                                       |
| Настройки                 | ✅ работает | чтение/запись настроек ок                                                                         |

### Чего не хватает (общесистемно)

- Нет видимости состояния движка: пользователь не понимает, что воркер офлайн.
- Сбой диспатча глушится — нет честной ошибки на «Старт».
- Нет скрипта одной командой поднять всё локально (api+worker) без Redis.

---

## ЭТАП 2 — План работ (приоритет: сначала чиним сломанное)

### 🔴 Task 1 — Локальный движок фоновых задач (filesystem-брокер + воркер) — ~40 мин

- **Что**: в `app/tasks/celery_app.py` — если `CELERY_BROKER_URL` начинается с
  `filesystem://`, автоматически настроить `broker_transport_options` (папки очереди под
  `data/broker/`) и file-result-backend. Для `redis://` поведение не меняется (обратная
  совместимость с проддом). Добавить `scripts/dev-up.sh` / `scripts/dev-down.sh` —
  поднять uvicorn + celery worker одной командой локально, без Redis. Обновить `.env.example`.
- **Раздел**: инфраструктура (влияет на все разделы со «Стартом»).
- **Результат**: `./scripts/dev-up.sh` поднимает api+worker; диспатченная задача реально
  исполняется воркером (проверяю `pup_tg.echo` + одну доменную задачу).
- **Оценка**: 40 мин.

### 🔴 Task 2 — Честный статус движка + баннер в UI — ~30 мин

- **Что**: `GET /api/v1/system/status` — `broker_reachable` + `worker_online`
  (celery control ping, короткий таймаут). В `public/index.html` — поллинг и
  ненавязчивый баннер «⚠ Фоновый воркер офлайн — задачи не будут исполняться», скрытый
  когда воркер онлайн.
- **Раздел**: общий (хедер/дашборд).
- **Результат**: воркер выключен → баннер виден; включён → скрыт.
- **Оценка**: 30 мин.

### 🟠 Task 3 — Не глушить сбой диспатча на «Старт» — ~25 мин

- **Что**: общий хелпер `dispatch_task()` — при недоступном брокере не оставлять задачу
  `ACTIVE` молча, а вернуть `503` с понятным сообщением (UI покажет toast). Применить к
  самым ходовым стартам: парсер, нейрокомментинг, AI-продажник, рассылка в ЛС. Остальные —
  в TODO.
- **Раздел**: парсер / комментинг / ai-sales / dm-campaign.
- **Результат**: при выключенном воркере «Старт» даёт явную ошибку, задача остаётся в DRAFT.
- **Оценка**: 25 мин.

### 🟢 Task 4 — Дашборд: карточка «Статус движка» (если останется время) — ~15 мин

- **Что**: на дашборде маленькая карточка broker/worker по `/system/status`.
- **Результат**: статус движка виден сразу на главной.

### TODO (вынесено за рамки 2ч)

- [x] **Готово** — `dispatch_task()` раскатан на ВСЕ диспатчи: 10 swallow-стартов
      (boost, stories-boost, cloner, converter, channel-creator, join-chats,
      invite, chat-broadcast, auto-replier, warmup-scripts) + рефактор уже-честных
      (ai-promoter, ai-sales start/monitor/reply, kb-crawl, kb-self-test,
      kb-check-conflicts, style import/scrape). Сырых `send_task` в app/api не осталось.

### Добивка хвостов (сессия 2026-06-02 #2, бюджет ~3ч)

Данные позволяют живой прогон: 3 ACTIVE-аккаунта с ACTIVE-прокси (+79587295541 и др.).

- [ ] **1. Beat в dev-up.sh** (~15м, низкий риск). Поднимать `celery beat` вместе с
      api+worker; dev-down — гасить. Результат: периодические задачи (heartbeat, reaper,
      membership-check) реально тикают; в логах beat виден heartbeat раз в минуту.
- [ ] **2. Карточка статуса движка на дашборде** (~30м). Аккуратно добавить карточку
      broker/worker по `GET /system/status` в rDash, не трогая существующий layout.
      Результат: на дашборде видно «Движок: воркер онлайн/офлайн». Регресс дашборда 0.
- [ ] **3. H3 — pause/cancel в процессе** (~45м). Кооперативная проверка статуса задачи
      в цикле по источникам (`revoke(terminate)` на threads-пуле не убивает поток).
      При PAUSED/CANCELLED — прекратить обработку оставшихся источников, сохранить
      частичный прогресс, не помечать COMPLETED. Результат: на живом воркере многосорсный
      парс реально останавливается между источниками.
- [ ] **4. M4 — ротация аккаунтов** (~60-90м, выше риск). Вместо одного аккаунта на все
      источники — раскладывать источники по нескольким ACTIVE-аккаунтам. Риск: переписка
      жизненного цикла клиента Telethon. Делаю если останется время; иначе TODO с причиной.
- [ ] **5. Live-тест прогрева (DEBT 2.4)** (~30м). Реально запустить прогрев на живом
      аккаунте, проверить что воркер исполняет и пишет лог действий. Результат: подтвердить
      работу или зафиксировать конкретные баги.

---

## ЭТАП 3 — Журнал выполнения

- ✅ **Task 1** (`a83d6dd`) — filesystem-брокер в `celery_app.py` + `scripts/dev-up.sh`/
  `dev-down.sh` + `.env.example`. Проверено: воркер исполняет `pup_tg.echo` и `ai_agent`.
  По ходу: переключил пул воркера на `threads` (solo блокировал воркер долгой агентной
  задачей; threads даёт реальный параллелизм для IO-bound нагрузки).
- ✅ **Task 2** (`eb6fc9b`) — `GET /api/v1/system/status` + баннер в `index.html`.
  Проверено playwright: воркер онлайн → баннер скрыт; офлайн → виден.
- ✅ **Task 3** (`4261846`) — `app/tasks/dispatch.py` (fail-fast → 503) применён к
  parser/dm-campaign/commenting (диспатч до смены статуса). Проверено: мёртвый брокер →
  503 за ~2с, задача остаётся PENDING; живой → парсер доходит до воркера и COMPLETED.
- ✅ **Доп.** (`d2f6bc0`) — быстрый детект воркера по pid-файлу (онлайн-статус ~0.05с
  вместо ~3с, без pidbox-мусора в очереди), ping — фоллбэк для прода.
- ✅ **Регресс** — повторный playwright-прогон всех 26 экранов: 26/26 чисто, ничего не
  сломано правками `index.html`.
- ⏭️ **Task 4** — отложено в TODO (см. выше).
