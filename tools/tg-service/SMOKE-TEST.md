# TG Service · SMOKE-TEST (прод, после первого деплоя)

> План прод-смоука. Фокус — **то, что НЕ удалось проверить локально** в dev:
> реальный Telegram-инбаунд/прокси/отправка, прогрев fastembed-модели, и
> поведение воркера под нагрузкой/рестартом. Каждый пункт: что проверяем →
> как → ожидаемо → что делать при провале.
>
> В dev зелёные: `regression_screens.py` 26/26, `contract_smoke.py` (211 routes),
> 18 unit. Здесь — только прод-специфика.
>
> ⚠️ Делать на **тестовом/безопасном** workspace и тестовых диалогах. Никаких
> реальных рассылок живым людям. Atlas-персону и аккаунты вернуть в исходный статус.

---

## 0. База: сервис поднялся

- [ ] `pm2 status` → `pup-tg-api`/`pup-tg-worker`/`pup-tg-beat` online.
- [ ] `curl .../tg-service/api/v1/system/status -H 'x-admin-token:…'` → 200.
- [ ] UI открывается в iframe (`/tg-service/?workspace=<ws>`), нет JS-/CSP-ошибок.
- [ ] Логи чисты: `grep -iE "Traceback|migration_failed|ImportError" logs/*` — пусто.
- [ ] Воркспейс-БД создалась (миграции прошли): в логах `migration_applied`, нет `migration_failed`.

## 1. fastembed-модель + гибридный RAG (P6-01) — dev: только локально

- [ ] `GET /api/v1/system/status` или загрузка KB-документа → затем `POST /api/v1/kb/search {"query":"…"}` → ответ с `"mode":"hybrid"` (а не `"keyword"`).
- [ ] Если первый запрос медленный/таймаут — это разовая загрузка модели (~0.22ГБ). После прогрева — быстро.
- [ ] Бэкфилл эмбеддингов на существующих чанках: `POST /api/v1/kb/reembed` → `{embedded:N}`.
- **Провал (нет сети к HF):** `mode:"keyword"` — приемлемо как fallback; решить, прогревать ли (Q4 в DEPLOY-CHECKLIST). Сервис не должен падать.

## 2. Реальная проверка прокси: auth + Telegram DC + гео (P6-08) — dev: было на реальных прокси, повторить на проде

- [ ] `POST /api/v1/proxies/{id}/check` на рабочем прокси → `status:ACTIVE`, `last_latency_ms>0`, `metadata.geo` заполнен (egress_ip/country/city).
- [ ] На заведомо мёртвом → `DEAD` с понятной ошибкой.
- [ ] `POST /api/v1/proxies/check-all` → корректная сводка alive/dead.
- **Провал:** проверить сетевой доступ VPS к Telegram DC (149.154.167.51:443 и т.д.) и к ip-api.com (гео).

## 3. Реальный аккаунт-чек + health-timeline (P6-07) — dev: только NO_PROXY

- [ ] `POST /api/v1/accounts/{id}/check-telegram` на живом аккаунте с прокси → `status:ACTIVE`, `success:true`, флаги (restricted/scam/fake), humanity_score.
- [ ] Вкладка «Здоровье» аккаунта → строка таймлайна (source=manual).
- [ ] Beat-авточек: дождаться `pup_tg.account_health_check` (раз в 6ч) или дёрнуть вручную → строки `source=scheduled`, анти-бан-пауза между аккаунтами в логах.
- **Провал:** проверить, что аккаунт реально жив и прокси привязан; сверить с логами воркера.

## 4. AI-Агент: входящие ЛС, секретарь, воронка-стадии, хэндофф (P5-05/P6-09) — dev: НЕТ инбаунда

> Самое непроверяемое в dev. На безопасном тестовом аккаунте/диалоге.

- [ ] Персона ACTIVE, `dm_enabled`, прокси привязан. Написать на аккаунт **с тестового** TG-аккаунта.
- [ ] Агент отвечает как секретарь (берёт историю, summary, KB/RAG, анти-бан-пауза) → запись в `tg_dm_threads`, лог `📩 ЛС`.
- [ ] **Единоличное владение ЛС:** убедиться, что auto-replier/ai-sales НЕ отвечают параллельно (gate `dm_ownership`): при активной персоне-владельце `POST /ai-sales/monitor` → 409; auto-replier yield в логах.
- [ ] **Воронка-стадии (P6-09):** привязать персоне `funnel_script_id` (скрипт с stages+advance_keywords) → написать сообщение с advance-словом → стадия в `tg_dm_threads.funnel_stage` сдвигается; `GET /ai-promoter/personas/{id}/funnel-stats` показывает распределение; на терминальной стадии — лог `funnel_terminal_reached`.
- [ ] **auto-replier HANDOFF → Агент:** на аккаунте с активным сценарием auto-replier и триггером HANDOFF_SALES — диалог НЕ получает шаблонный ответ, остаётся непрочитанным (лог `[HANDOFF → AI Агент]`).
- **Провал:** НЕ чинить на живом потоке — поставить персону на PAUSE, снять диагностику из логов, эскалировать. Atlas вернуть в ACTIVE.

## 5. Непрерывность воркеров: orphan-recovery при рестарте (P5-09) — dev: только unit

- [ ] Запустить длительную задачу (commenting/auto_replier/boost/cloner) → в середине `pm2 restart pup-tg-worker`.
- [ ] Через ≤ интервала beat `pup_tg.worker_continuity` (5 мин) задача с протухшим `last_tick_at` **переподхватывается** (лог `continuity_revived`), без дублей (живая петля держит heartbeat).
- [ ] AI-агент после рестарта: ровно ОДНА живая петля — следить, что `ai_agent_loop_superseded` гасит лишний revive и **нет** транзиентного `InterfaceError` в `_log_activity` (была гонка self-heal; под prefork каждый процесс со своим sqlite-коннектом — должно быть стабильнее, но проверить).
- **Провал:** зафиксировать лог гонки; при повторе — снизить `--concurrency` worker'а / разнести beat-тики.

## 6. Реальная отправка: bulk-apply профилей, approve коммента — dev: только NO_PROXY

- [ ] **Bulk-generate** профилей (Claude, без TG) на пуле тестовых аккаунтов → профили заполнились (это работало в dev).
- [ ] **Bulk-apply** на 1 тестовый аккаунт с прокси → имя/username/фото реально применились в TG (анти-бан-пауза между аккаунтами в логах).
- [ ] **Модерация комментов (P6-11):** задача с approval_mode=ALL → коммент в очереди (`GET /commenting/queue`) → `approve` реально отправляет в обсуждение поста; `reject` → REJECTED.
- **Провал:** проверить прокси/права аккаунта; отправка идёт только при ACTIVE-прокси (NO_PROXY-guard).

## 7. Arena self-play → харвест в Style Bank (P6-10) — dev: только синтетика

- [ ] Создать арену (тестовая TG-группа, 2–4 персоны, аккаунты в группе) → `start` → персоны реально переписываются (анти-бан cadence).
- [ ] `POST /arenas/{id}/harvest` → удачные реплики попадают в Style Bank (`total_harvested` растёт, дедуп при повторе); `GET /arenas/{id}/messages` — live-просмотр.
- **Провал:** проверить членство аккаунтов в группе (`/arenas/{id}/verify`) и прокси.

## 8. Уведомления админа + гейтинг (P5-08/P2-10b) — dev: бот не настроен

- [ ] При настроенных `NOTIFY_BOT_TOKEN`/`NOTIFY_CHAT_ID`: спровоцировать событие (или дождаться) → уведомление приходит в админ-чат.
- [ ] Выключить флаг (напр. `notify_on_spam_block`) в Настройках → событие НЕ шлёт уведомление (гейт `notify_admin_pref`).
- **Провал:** проверить токен/chat_id бота; без них notify — штатный no-op (не ошибка).

## 9. AI-бюджет + стоимость

- [ ] После серии AI-вызовов — `ai_spent_this_month_usd` в настройках растёт; при достижении `ai_monthly_limit_usd` AI-функции пропускаются (budget-gate).

---

## По итогам смоука

- [ ] Тестовые записи удалить; тестовые арены/задачи/скрипты — снять.
- [ ] Аккаунты и Atlas-персона — в исходный статус.
- [ ] Зафиксировать, что прошло / что отложено, в этот файл или WORK-PLAN.
